{
  "scripts": {
    "start": "cross-env NODE_ENV=development postcss --config build/ src/style.css -o dist/style.css -w",
    "build": "cross-env NODE_ENV=production postcss --config build/ src/style.css -o dist/style.css"
  },
  "dependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "autoprefixer": "^9.6.4",
    "cross-env": "^6.0.3",
    "cssnano": "^4.1.10",
    "postcss": "^7.0.18",
    "postcss-cli": "^6.1.3",
    "postcss-nesting": "^7.0.1",
    "tailwindcss": "^1.1.2"
  }
}
